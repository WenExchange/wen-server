'use strict';

/**
 * airdrop-distribution-stat router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::airdrop-distribution-stat.airdrop-distribution-stat');
