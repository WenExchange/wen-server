'use strict';

/**
 * airdrop-distribution-stat controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::airdrop-distribution-stat.airdrop-distribution-stat');
